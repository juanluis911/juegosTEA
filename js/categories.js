// 🎯 JavaScript para páginas de categorías
console.log('📂 Módulo de categorías cargado');

// Funciones específicas para categorías
function filterByAge(age) {
    console.log(Filtrando por edad: );
    // Implementar filtrado por edad
}

function launchGame(gameId) {
    console.log(Lanzando juego: );
    showNotification(Cargando juego ...);
    // Implementar navegación a juegos
}

// Inicialización de categorías
document.addEventListener('DOMContentLoaded', function() {
    console.log('✅ Página de categoría inicializada');
});
